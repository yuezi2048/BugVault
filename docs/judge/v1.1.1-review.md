# BugVault v1.1.1 技术评审报告

> 评审日期: 2026-06-04
> 评审范围: v1.0.0 → v1.1.1（23 commits, 36 files, +4110/-407 lines）
> 评审人: Reasonix Code（架构审计）

---

## 总体评级：B+（良好，有改进空间）

| 维度 | 评分 | 说明 |
|---|---|---|
| 架构设计 | A- | 策略模式、Small-to-Big RAG、双重降级设计正确 |
| 代码质量 | B+ | 部分重复代码（LanceDBClient 重复方法） |
| 测试覆盖 | A | 70 项 pytest，覆盖主要链路 |
| 容灾能力 | A | 双重降级、FTS 降级、chunks 降级三重保护 |
| 性能 | B | ONNX embedding 是瓶颈，但零向量优化已到位 |
| 文档 | A | 架构文档、ADR、RFC 均完备 |

---

## 发现的 P1 问题（4 项，无 P0）

| # | 问题 | 文件 | 影响 | 建议修复 |
|---|------|------|------|----------|
| 1 | **LanceDBClient 重复方法定义** `search_chunks_fts` 和 `upsert_chunks` 各定义两次，第二次覆盖第一次 | `lancedb_client.py:105+197`, `:124+287` | 运行时无影响（后面赢），维护风险 | 清理重复定义，合并为一个版本 |
| 2 | **零向量 Parent 回退检索结果随机** chunks 为空时降级到 `bug_records`，所有 vector 为 `[0.0]*512`，ANN 排序无意义 | `tools.py` (fallback path) | 首次启动/空库时检索结果不可靠 | 降级时改用 FTS 而非向量搜索 |
| 3 | **默认 Cross-Encoder 仅英文** `Xenova/ms-marco-MiniLM-L6-v2` 不处理中文，但 BugVault 数据含中文排障 | `config.py` | 中文排障记录精排不准确 | 默认改为多语言模型 `jina-reranker-v2-base-multilingual` |
| 4 | **Embedding 维度切换不兼容** 表 schema 固定了 vector 维度（512），切换模型后插入报错 | `lancedb_client.py` | 用户换模型后灌库崩溃 | `_init_table` 中验证维度一致性 |

---

## 架构完整性检查

### ✅ 检索链路

```
Query → chunks ANN + FTS → RRF(k=60) → parent_id 去重 → IN 回捞 → Cross-Encoder → Top-K
```

状态: **完整实现并验证通过**

### ✅ 评估链路

```
eval_depth="claim_level"
  → 配额检查 → try/except 包裹
  → 成功: claims_analysis[] + faithfulness + suggested_action
  → 失败: fallback simple ("simple (fallback_from_error)")
```

状态: **完整实现，双重降级保护**

### ✅ 存储层

| 表 | 用途 | 向量 | 状态 |
|---|---|---|---|
| `bug_records` | 完整文档存储 | `[0.0]*512`（零向量） | ✅ |
| `bugvault_chunks` | 检索入口 | 真实 embedding | ✅ |
| FTS 索引 | BM25 双路召回 | 双表均已构建 | ✅ |

### ✅ 容灾

| 故障场景 | 保护机制 | 状态 |
|---|---|---|
| claim_level LLM 崩溃 | 双重降级 → simple | ✅ |
| FTS 引擎异常 | try/except → vector-only | ✅ |
| chunks 表为空 | 降级 bug_records 直接检索 | ✅（但结果随机，P1） |
| Max claim evals 超限 | 配额熔断 → simple | ✅ |

### ✅ 数据导入

| 来源 | 方法 | 父表 | Chunk 表 |
|---|---|---|---|
| 本地归档 | `scripts/rebuild_index.py` | ✅ 119 条 | ✅ 238 条 |
| Stack Overflow | `scripts/import_external.py` | ✅ 零向量 | ✅ 真实 embedding |

---

## 否决方案回顾

| 方案 | 否决理由 | 文档 |
|---|---|---|
| ColBERT 重排 | ~1.5GB PyTorch 依赖，5-20 条候选集 CE 更优 | `adr-cross-encoder-vs-colbert.md` |
| Sentence Window Retrieval | 数据量未达阈值，回捞 parent 已覆盖 | `rfc-002-sentence-window.md` |
| 智能去重 | `fetch_records_by_ids` 天然去重 | `rfc-001-smart-dedup.md` |
| Score Fusion | 量纲不可公度，RRF 更鲁棒 | `v1.1.1-technical-decisions.md` |
| LangChain/LangGraph | 线性 CRUD 用框架无收益 | `docs/refer/分析/04.为什么选择SDK.md` |

---

## 改进建议（非 P0/P1）

| 建议 | 理由 |
|---|---|
| `scripts/import_external.py` 增加进度条 | 全量导入 ~17 分钟，当前只有静默等待 |
| 默认 Cross-Encoder 改为 `jina-reranker-v2-base-multilingual` | 多语言支持，但 ~1.1GB 需考虑下载成本 |
| 增加 `scripts/verify_import.sh` 快速验证脚本 | 当前需要手动跑 Python 命令验证 |
| README 添加 Small-to-Big 架构图 | 当前文字描述不够直观 |
